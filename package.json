{
  "name": "azure_example",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server",
    "dev": "concurrently \"nodemon -r dotenv/config server.js\" \"cd frontend && npm run start\"",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongod": "^2.0.0",
    "mongoose": "^5.9.20",
    "node": "^14.4.0"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
